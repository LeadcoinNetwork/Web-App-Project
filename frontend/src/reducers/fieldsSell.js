import types from "../actions/types"
import fieldsSellData from "./fieldsSellData"

const initialState = fieldsSellData

const fieldsSell = (state = initialState, action) => {
  return state
}

export default fieldsSell
