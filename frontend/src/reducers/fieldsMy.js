import types from "../actions/types"
import fieldsMyData from "./fieldsMyData"

const initialState = fieldsMyData

const fieldsMy = (state = initialState, action) => {
  return state
}

export default fieldsMy
