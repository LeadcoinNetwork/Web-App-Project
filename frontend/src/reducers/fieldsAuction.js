import types from "../actions/types"
import fieldsAuctionData from "./fieldsAuctionData"

const initialState = fieldsAuctionData

const fieldsAuction = (state = initialState, action) => {
  return state
}

export default fieldsAuction
